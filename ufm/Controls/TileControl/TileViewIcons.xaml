<!--<UserControl
    x:Class="ufm.TileViewIcons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ufm="using:ufm"
    xmlns:viewModel="using:Core_FileManagement"
    x:Name="RootControl"
    Loaded="OnLoaded"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="TileGridViewItemStyle" TargetType="GridViewItem">
            <Setter Property="Margin" Value="1" />
            <Setter Property="Padding" Value="1" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="CornerRadius" Value="4" />
        </Style>

    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <GridView
            x:Name="ItemsGridView"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            IsItemClickEnabled="True"
            ItemClick="ItemsGridView_OnItemClick"
            ItemContainerStyle="{StaticResource TileGridViewItemStyle}"
            ItemsSource="{Binding ExplorerItems}"
            SelectionMode="Extended">

            <GridView.ItemTemplateSelector>
                <ufm:ListViewItemTemplateSelector>
                    <ufm:ListViewItemTemplateSelector.MyComputerTemplate>
                        <DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
                            <ufm:TileMyPcUC
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
                        </DataTemplate>
                    </ufm:ListViewItemTemplateSelector.MyComputerTemplate>

                    <ufm:ListViewItemTemplateSelector.DriveTemplate>
                        <DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
                            <ufm:TileMyDriveUc
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
                        </DataTemplate>
                    </ufm:ListViewItemTemplateSelector.DriveTemplate>

                    <ufm:ListViewItemTemplateSelector.FolderTemplate>
                        <DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
                            <ufm:TileMyFoldersUc
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
                        </DataTemplate>
                    </ufm:ListViewItemTemplateSelector.FolderTemplate>

                    <ufm:ListViewItemTemplateSelector.FileTemplate>
                        <DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
                            <ufm:TileMyFiles
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
                        </DataTemplate>
                    </ufm:ListViewItemTemplateSelector.FileTemplate>

                </ufm:ListViewItemTemplateSelector>
            </GridView.ItemTemplateSelector>

            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid
                        ItemHeight="{Binding ItemHeight, ElementName=RootControl}"
                        ItemWidth="{Binding ItemWidth, ElementName=RootControl}"
                        MaximumRowsOrColumns="24"
                        Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>
    </Grid>
</UserControl>-->


<!--<UserControl
    x:Class="ufm.TileViewIcons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ufm="using:ufm"
    xmlns:viewModel="using:Core_FileManagement"
    x:Name="RootControl"
    Loaded="OnLoaded"
    mc:Ignorable="d">

	<UserControl.Resources>
		--><!-- Стиль для GridViewItem с поддержкой выделения --><!--
		<Style x:Key="TileGridViewItemStyle" TargetType="GridViewItem">
			<Setter Property="Margin" Value="1" />
			<Setter Property="Padding" Value="1" />
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="HorizontalContentAlignment" Value="Stretch" />
			<Setter Property="VerticalContentAlignment" Value="Stretch" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
			<Setter Property="CornerRadius" Value="4" />
			--><!-- Добавляем DoubleTapped событие --><!--
			<Setter Property="IsDoubleTapEnabled" Value="True" />

			--><!-- Visual States для выделения --><!--
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="GridViewItem">
						<Grid x:Name="RootGrid">
							<VisualStateManager.VisualStateGroups>
								<VisualStateGroup x:Name="CommonStates">
									<VisualState x:Name="Normal" />
									<VisualState x:Name="PointerOver">
										<Storyboard>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBackground" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="0.2" />
											</ObjectAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
									<VisualState x:Name="Selected">
										<Storyboard>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBackground" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="0.4" />
											</ObjectAnimationUsingKeyFrames>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectedBorder" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="1" />
											</ObjectAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
									<VisualState x:Name="PointerOverSelected">
										<Storyboard>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBackground" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="0.4" />
											</ObjectAnimationUsingKeyFrames>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectedBorder" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="1" />
											</ObjectAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
								</VisualStateGroup>
								<VisualStateGroup x:Name="FocusStates">
									<VisualState x:Name="Focused">
										<Storyboard>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="FocusVisual" Storyboard.TargetProperty="Visibility">
												<DiscreteObjectKeyFrame KeyTime="0" Value="Visible" />
											</ObjectAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
									<VisualState x:Name="Unfocused" />
								</VisualStateGroup>
							</VisualStateManager.VisualStateGroups>

							--><!-- Фон выделения --><!--
							<Rectangle x:Name="SelectionBackground"
                                       Fill="{ThemeResource SystemControlHighlightListAccentLowBrush}"
                                       Opacity="0"
                                       RadiusX="4"
                                       RadiusY="4" />

							--><!-- Рамка выделения --><!--
							<Border x:Name="SelectedBorder"
                                    BorderBrush="{ThemeResource SystemControlHighlightAccentBrush}"
                                    BorderThickness="2"
                                    CornerRadius="4"
                                    Opacity="0" />

							--><!-- Контент --><!--
							<ContentPresenter x:Name="ContentPresenter"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}" />

							--><!-- Фокусная рамка --><!--
							<Rectangle x:Name="FocusVisual"
                                       Stroke="{ThemeResource SystemControlFocusVisualPrimaryBrush}"
                                       StrokeThickness="2"
                                       StrokeDashArray="1,1"
                                       RadiusX="4"
                                       RadiusY="4"
                                       Visibility="Collapsed" />
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
		<GridView
            x:Name="ItemsGridView"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            IsItemClickEnabled="True"
            ItemClick="ItemsGridView_OnItemClick"
            ItemContainerStyle="{StaticResource TileGridViewItemStyle}"
            ItemsSource="{Binding ExplorerItems}"
            SelectionMode="Extended"
            DoubleTapped="ItemsGridView_DoubleTapped">
			--><!-- SelectionMode="Single" для выделения одного элемента -->
			<!-- DoubleTapped для обработки двойного клика --><!--

			<GridView.ItemTemplateSelector>
				<ufm:ListViewItemTemplateSelector>
					<ufm:ListViewItemTemplateSelector.MyComputerTemplate>
						<DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
							<ufm:TileMyPcUC
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
						</DataTemplate>
					</ufm:ListViewItemTemplateSelector.MyComputerTemplate>

					<ufm:ListViewItemTemplateSelector.DriveTemplate>
						<DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
							<ufm:TileMyDriveUc
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
						</DataTemplate>
					</ufm:ListViewItemTemplateSelector.DriveTemplate>

					<ufm:ListViewItemTemplateSelector.FolderTemplate>
						<DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
							<ufm:TileMyFoldersUc
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
						</DataTemplate>
					</ufm:ListViewItemTemplateSelector.FolderTemplate>

					<ufm:ListViewItemTemplateSelector.FileTemplate>
						<DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
							<ufm:TileMyFiles
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
						</DataTemplate>
					</ufm:ListViewItemTemplateSelector.FileTemplate>

				</ufm:ListViewItemTemplateSelector>
			</GridView.ItemTemplateSelector>

			<GridView.ItemsPanel>
				<ItemsPanelTemplate>
					<ItemsWrapGrid
                        ItemHeight="{Binding ItemHeight, ElementName=RootControl}"
                        ItemWidth="{Binding ItemWidth, ElementName=RootControl}"
                        MaximumRowsOrColumns="24"
                        Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</GridView.ItemsPanel>
		</GridView>
	</Grid>
</UserControl>-->

<UserControl
    x:Class="ufm.TileViewIcons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ufm="using:ufm"
    xmlns:viewModel="using:Core_FileManagement"
    x:Name="RootControl"
    Loaded="OnLoaded"
    mc:Ignorable="d">

	<UserControl.Resources>
		<!-- Стиль для GridViewItem с поддержкой выделения -->
		<Style x:Key="TileGridViewItemStyle" TargetType="GridViewItem">
			<Setter Property="Margin" Value="1" />
			<Setter Property="Padding" Value="1" />
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="HorizontalContentAlignment" Value="Stretch" />
			<Setter Property="VerticalContentAlignment" Value="Stretch" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
			<Setter Property="CornerRadius" Value="4" />
			<!-- Добавляем DoubleTapped событие -->
			<Setter Property="IsDoubleTapEnabled" Value="True" />

			<!-- Visual States для выделения -->
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="GridViewItem">
						<Grid x:Name="RootGrid">
							<VisualStateManager.VisualStateGroups>
								<VisualStateGroup x:Name="CommonStates">
									<VisualState x:Name="Normal" />
									<VisualState x:Name="PointerOver">
										<Storyboard>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBackground" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="0.2" />
											</ObjectAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
									<VisualState x:Name="Selected">
										<Storyboard>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBackground" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="0.4" />
											</ObjectAnimationUsingKeyFrames>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectedBorder" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="1" />
											</ObjectAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
									<VisualState x:Name="PointerOverSelected">
										<Storyboard>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBackground" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="0.4" />
											</ObjectAnimationUsingKeyFrames>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectedBorder" Storyboard.TargetProperty="Opacity">
												<DiscreteObjectKeyFrame KeyTime="0" Value="1" />
											</ObjectAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
								</VisualStateGroup>
								<VisualStateGroup x:Name="FocusStates">
									<VisualState x:Name="Focused">
										<Storyboard>
											<ObjectAnimationUsingKeyFrames Storyboard.TargetName="FocusVisual" Storyboard.TargetProperty="Visibility">
												<DiscreteObjectKeyFrame KeyTime="0" Value="Visible" />
											</ObjectAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
									<VisualState x:Name="Unfocused" />
								</VisualStateGroup>
							</VisualStateManager.VisualStateGroups>

							<!-- Фон выделения -->
							<Rectangle x:Name="SelectionBackground"
                                       Fill="{ThemeResource SystemControlHighlightListAccentLowBrush}"
                                       Opacity="0"
                                       RadiusX="4"
                                       RadiusY="4" />

							<!-- Рамка выделения - привязываем размер к иконке -->
							<Border x:Name="SelectedBorder"
                                    BorderBrush="{ThemeResource SystemControlHighlightAccentBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Opacity="0"
                                    Width="{Binding ItemWidth, ElementName=RootControl}"
                                    Height="{Binding ItemHeight, ElementName=RootControl}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />

							<!-- Контент -->
							<ContentPresenter x:Name="ContentPresenter"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}" />

							<!-- Фокусная рамка -->
							<Rectangle x:Name="FocusVisual"
                                       Stroke="{ThemeResource SystemControlFocusVisualPrimaryBrush}"
                                       StrokeThickness="2"
                                       StrokeDashArray="1,1"
                                       RadiusX="4"
                                       RadiusY="4"
                                       Visibility="Collapsed" />
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
		<GridView
            x:Name="ItemsGridView"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            IsItemClickEnabled="True"
            ItemClick="ItemsGridView_OnItemClick"
            ItemContainerStyle="{StaticResource TileGridViewItemStyle}"
            ItemsSource="{Binding ExplorerItems}"
            SelectionMode="Single"
            DoubleTapped="ItemsGridView_DoubleTapped">
			<!-- SelectionMode="Single" для выделения одного элемента -->
			<!-- DoubleTapped для обработки двойного клика -->

			<GridView.ItemTemplateSelector>
				<ufm:ListViewItemTemplateSelector>
					<ufm:ListViewItemTemplateSelector.MyComputerTemplate>
						<DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
							<ufm:TileMyPcUC
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
						</DataTemplate>
					</ufm:ListViewItemTemplateSelector.MyComputerTemplate>

					<ufm:ListViewItemTemplateSelector.DriveTemplate>
						<DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
							<ufm:TileMyDriveUc
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
						</DataTemplate>
					</ufm:ListViewItemTemplateSelector.DriveTemplate>

					<ufm:ListViewItemTemplateSelector.FolderTemplate>
						<DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
							<ufm:TileMyFoldersUc
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
						</DataTemplate>
					</ufm:ListViewItemTemplateSelector.FolderTemplate>

					<ufm:ListViewItemTemplateSelector.FileTemplate>
						<DataTemplate x:DataType="viewModel:ExplorerItemViewModel">
							<ufm:TileMyFiles
                                Width="{Binding ItemWidth, ElementName=RootControl}"
                                Height="{Binding ItemHeight, ElementName=RootControl}"
                                DisplayMode="Vertical" />
						</DataTemplate>
					</ufm:ListViewItemTemplateSelector.FileTemplate>

				</ufm:ListViewItemTemplateSelector>
			</GridView.ItemTemplateSelector>

			<GridView.ItemsPanel>
				<ItemsPanelTemplate>
					<ItemsWrapGrid
                        ItemHeight="{Binding ItemHeight, ElementName=RootControl}"
                        ItemWidth="{Binding ItemWidth, ElementName=RootControl}"
                        MaximumRowsOrColumns="24"
                        Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</GridView.ItemsPanel>
		</GridView>
	</Grid>
</UserControl>